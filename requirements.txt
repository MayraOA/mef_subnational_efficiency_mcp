# MEF Subnational Efficiency MCP — Dependencies
# Generated for: mef_subnational_efficiency_mcp

# ── Core data ────────────────────────────────────────────────────────────────
pandas>=2.2.0
pyarrow>=15.0.0          # Parquet I/O
numpy>=1.26.0

# ── HTTP / async ─────────────────────────────────────────────────────────────
httpx>=0.27.0

# ── MCP Server ───────────────────────────────────────────────────────────────
mcp>=1.0.0               # Model Context Protocol SDK

# ── OCR (Track 1964) ─────────────────────────────────────────────────────────
paddleocr>=2.7.3
paddlepaddle>=2.6.0      # CPU build; use paddlepaddle-gpu for GPU
pdf2image>=1.16.3        # PDF → image conversion for PaddleOCR
Pillow>=10.0.0
# poppler-utils          # NO es un paquete de PyPI: es dependencia de SISTEMA de pdf2image.
#                        # Instalar aparte: Ubuntu/Debian `apt-get install poppler-utils`;
#                        # macOS `brew install poppler`; Windows: binarios de poppler en PATH.

# ── Dashboard ────────────────────────────────────────────────────────────────
streamlit>=1.35.0
plotly>=5.20.0
altair>=5.3.0
matplotlib>=3.8.0        # requerido por Styler.background_gradient (Tab 3)
streamlit-extras>=0.4.0  # st.metric con delta styling extra

# ── Geospatial (Tab 2) ────────────────────────────────────────────────────────
# El mapa del Tab 2 usa plotly.express.choropleth + un GeoJSON (json puro).
# No se requieren geopandas/folium; se dejan comentados para mantener el install liviano.
# geopandas>=0.14.0
# folium>=0.15.0
# streamlit-folium>=0.20.0

# ── Optional fast processing ──────────────────────────────────────────────────
# polars>=0.20.0          # alternativa a pandas para datasets muy grandes
# duckdb>=0.10.0          # SQL directo sobre Parquet/CSV sin carga completa

# ── Dev / testing ─────────────────────────────────────────────────────────────
pytest>=8.0.0
python-dotenv>=1.0.0
